  const app = Vue.createApp({
   data() {
    return {
     message: "Hello WEBPROG AFD241 World! With separate CSS and JS files",
     vueClass: "pinkBG"

    }
   }
  })
  
  app.mount('#app')
